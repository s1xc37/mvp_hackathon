// Display speeds on the marker badges (purely cosmetic — actual sim duration is driven
// by road.repair_hours and route.duration_min, scaled by SimClock speed).
export const TRUCK_DISPLAY_KMH = 60;
export const PAVER_DISPLAY_KMH = 5;
export const SIM_TICK_MS = 200; // SimClock interval in real ms
